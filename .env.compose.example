# Copy this file to .env.compose and replace change_me with the existing
# PostgreSQL password before running Docker Compose.
POSTGRES_USER=smartdoc_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=smartdoc_db
